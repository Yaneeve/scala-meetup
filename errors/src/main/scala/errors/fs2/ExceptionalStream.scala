package errors.fs2

import cats.effect.IO
import fs2.{Stream, StreamApp}

//object ExceptionalStream extends StreamApp[IO]{
//  override def stream(args: List[String], requestShutdown: IO[Unit]): fs2.Stream[IO, StreamApp.ExitCode] = {
//    Stream.
//  }
//}
